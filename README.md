# rasp-pyton-sheets
raspberry pyton google sheets

python3 -m venv venv
source venv/bin/activate
pip install pyTelegramBotAPI gspread oauth2client google-api-python-client
pip install python-dotenv
python3 index.py

* auth_setup to generate oauth token json
